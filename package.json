{
  "name": "open-civilization-site",
  "version": "0.1.0",
  "private": true,
  "description": "Open Civilization podcast website",
  "scripts": {
    "dev": "vercel dev --listen 3000",
    "start": "npx serve public -l 3000",
    "validate": "npx html-validate public/index.html || true",
    "links": "npx linkinator public/index.html --silent || true"
  },
  "devDependencies": {
    "png-to-ico": "^3.0.1",
    "sharp": "^0.34.5"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.0.2"
  }
}
